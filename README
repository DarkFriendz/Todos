< ---------------------------------------- >

    -> Tarefas:

    -> Structure:
        - Criar site com flask (X)
        - Configurar flask (X)
        - Criar Pasta Templates e Static (X)
        - Criar Bluprint site (X)
        - Criar DB (X)
        - Terminar Python (X)
        - Criar HTML em Tamplates (X)
        - Terminar HTML (X)
        - Criar JS e CSS em static (X)
        - Terminar JS e CSS (X)

    -> Esquema
    
        - Tarefas em (.) a fazer
        - Tarefas em (X) feito

< ---------------------------------------- >

    -> Assets:

        - flask
        - sqlite3

< ---------------------------------------- >

    -> Run:

        - Excutar main.py

        - Usar url criada no CMD